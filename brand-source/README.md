# Brand source files

The originals the client supplied, kept out of `public/` so they are never served.

| File | Supplied | Notes |
|---|---|---|
| `nk-taher-logo-source.jpg` | 25 Aug 2026 | 1019×594 JPG, opaque white background. The only original we have — a vector is still requested (Requirements §2.1). |

`public/images/brand/*.png` are derived from this file. HANDOVER.md §4 records exactly how the
transparency was keyed, so the derivation can be repeated if a better original arrives.
